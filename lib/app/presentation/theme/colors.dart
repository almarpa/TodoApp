import 'package:flutter/material.dart';

const textColor = Color(0xFF4A4A4A);
const backgroundColor = Color(0xFF01545D);

const primary = Color(0xFF40B7AD);
const secondary = Color(0xFF53E39C);
const tertiary = Color(0xFF71FEBA);

const white = Colors.white;
const black = Colors.black;
const grey = Color(0xFF829788);
const errorColor = Colors.red;
const outlineColor = Color(0xFF424242);

const darkBlueColor = Color(0xFF10355B);
const greenOscure = Color(0xFF205A6D);
const greenLight = Color(0xFF008186);

const gradientColor = LinearGradient(
  colors: [
    primary,
    secondary,
  ],
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
);
